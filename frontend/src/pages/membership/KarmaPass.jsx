import { useEffect, useState } from 'react';
import { BadgeCheck, Check, Crown, LoaderCircle, Sparkles, WalletCards, Zap } from 'lucide-react';
import UserLayout from '../../layouts/UserLayout';
import { createMembership, getCurrentMembership } from '../../services/walletService';

const money = new Intl.NumberFormat('vi-VN');
const formatDate = (value) => value ? new Intl.DateTimeFormat('vi-VN', { dateStyle: 'medium' }).format(new Date(value)) : '—';

export default function KarmaPass() {
  const [membership, setMembership] = useState(null); const [method, setMethod] = useState('KARMA'); const [months, setMonths] = useState(1); const [loading, setLoading] = useState(true); const [submitting, setSubmitting] = useState(false); const [error, setError] = useState('');
  const karmaCost = months === 1 ? 100 : months === 3 ? 250 : 450; const priceVnd = months === 1 ? 50000 : months === 3 ? 120000 : 200000;
  const load = async () => { try { const data = await getCurrentMembership(); setMembership(data.membership); } catch (err) { setError(err.message); } finally { setLoading(false); } };
  useEffect(() => { load(); }, []);
  const submit = async () => { setSubmitting(true); setError(''); try { const data = await createMembership({ months, payment_method: method, ...(method === 'KARMA' ? { karma_cost: karmaCost } : { price_vnd: priceVnd }) }); if (data.payment_url) window.location.assign(data.payment_url); else { setMembership(data.membership); setSubmitting(false); } } catch (err) { setError(err.message); setSubmitting(false); } };
  return <UserLayout><div className="space-y-8 pb-12"><header><p className="mb-2 text-sm font-semibold uppercase tracking-[0.18em] text-amber-600">Membership</p><h1 className="text-3xl font-black tracking-tight text-slate-900 dark:text-white">Karma Pass</h1><p className="mt-2 max-w-2xl text-slate-500 dark:text-slate-400">Mở khóa trải nghiệm mượn đồ nhẹ nhàng hơn với ưu đãi cọc và quyền ưu tiên hiển thị.</p></header>{membership && <section className="overflow-hidden rounded-3xl border border-amber-200 bg-amber-50 p-6 dark:border-amber-900/50 dark:bg-amber-950/20 sm:p-8"><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-start"><div className="flex gap-4"><span className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-amber-400 text-white shadow-lg shadow-amber-400/20"><Crown size={23} /></span><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-black text-slate-900 dark:text-white">Karma Pass · {membership.status === 'PENDING' ? 'Đang chờ thanh toán' : 'Đang hoạt động'}</h2><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${membership.status === 'PENDING' ? 'bg-amber-200 text-amber-800' : 'bg-emerald-100 text-emerald-700'}`}>{membership.status}</span></div><p className="mt-1 text-sm text-slate-600 dark:text-slate-300">Gói hiện tại của bạn</p></div></div><div className="grid grid-cols-2 gap-4 text-sm sm:text-right"><div><p className="text-slate-500">Bắt đầu</p><p className="mt-1 font-bold text-slate-800 dark:text-white">{formatDate(membership.start_at)}</p></div><div><p className="text-slate-500">Hết hạn</p><p className="mt-1 font-bold text-slate-800 dark:text-white">{formatDate(membership.end_at)}</p></div></div></div></section>}{loading ? <div className="flex justify-center p-12"><LoaderCircle className="animate-spin text-emerald-600" /></div> : <div className="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-700 dark:bg-slate-800"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-50 text-emerald-700 dark:bg-emerald-900/30 dark:text-emerald-300"><Zap size={20} /></span><div><h2 className="font-bold text-slate-900 dark:text-white">Chọn thời hạn</h2><p className="text-sm text-slate-500">Gia hạn nối tiếp gói đang hoạt động.</p></div></div><div className="mt-6 grid grid-cols-3 gap-2">{[1, 3, 6].map((value) => <button key={value} onClick={() => setMonths(value)} className={`rounded-xl border px-2 py-3 text-center text-sm font-bold ${months === value ? 'border-emerald-500 bg-emerald-50 text-emerald-700 dark:bg-emerald-900/20' : 'border-slate-200 text-slate-600 dark:border-slate-600 dark:text-slate-300'}`}>{value} tháng</button>)}</div><div className="mt-8 space-y-3 text-sm"><p className="flex items-center gap-2 text-slate-600 dark:text-slate-300"><Check size={16} className="text-emerald-500" /> Miễn hoặc giảm phí đặt cọc</p><p className="flex items-center gap-2 text-slate-600 dark:text-slate-300"><Check size={16} className="text-emerald-500" /> Ưu tiên hiển thị bài đăng</p><p className="flex items-center gap-2 text-slate-600 dark:text-slate-300"><Check size={16} className="text-emerald-500" /> Gia hạn liền mạch</p></div></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-700 dark:bg-slate-800"><h2 className="font-bold text-slate-900 dark:text-white">Phương thức thanh toán</h2><div className="mt-5 grid gap-3 sm:grid-cols-2"><button onClick={() => setMethod('KARMA')} className={`rounded-xl border p-4 text-left ${method === 'KARMA' ? 'border-emerald-500 bg-emerald-50 dark:bg-emerald-900/20' : 'border-slate-200 dark:border-slate-600'}`}><WalletCards className="text-emerald-600" size={20} /><p className="mt-3 font-bold text-slate-900 dark:text-white">Trả bằng Karma</p><p className="mt-1 text-sm text-slate-500">{money.format(karmaCost)} Karma</p></button><button onClick={() => setMethod('PAYOS')} className={`rounded-xl border p-4 text-left ${method === 'PAYOS' ? 'border-emerald-500 bg-emerald-50 dark:bg-emerald-900/20' : 'border-slate-200 dark:border-slate-600'}`}><Sparkles className="text-blue-600" size={20} /><p className="mt-3 font-bold text-slate-900 dark:text-white">Trả bằng PayOS</p><p className="mt-1 text-sm text-slate-500">{money.format(priceVnd)} đ</p></button></div>{error && <p className="mt-4 rounded-lg bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<button onClick={submit} disabled={submitting} className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-slate-900 px-4 py-3.5 font-bold text-white hover:bg-emerald-700 disabled:opacity-60">{submitting ? 'Đang xử lý...' : method === 'KARMA' ? 'Đăng ký bằng Karma' : 'Tiếp tục với PayOS'}</button><div className="mt-5 flex items-center gap-2 text-xs text-slate-500"><BadgeCheck size={15} className="text-emerald-600" /> Thanh toán an toàn, trạng thái được cập nhật tự động.</div></section></div>}</div></UserLayout>;
}